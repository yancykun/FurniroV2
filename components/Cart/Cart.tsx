import Section from '../Layout/Section';
import CartTable from './CartTable';

const Cart = () => {
    return (
        <Section>
            <CartTable />
        </Section>
    );
};

export default Cart;
